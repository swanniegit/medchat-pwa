# Environment Configuration for MedChat PWA
# Copy this file to .env for local development

# Application Settings
ENVIRONMENT=development
HOST=0.0.0.0
PORT=8000

# Security Settings (generate new secrets for production)
SECRET_KEY=your-secret-key-here
JWT_SECRET=your-jwt-secret-here

# CORS Settings
FRONTEND_URL=http://localhost:3000

# Rate Limiting
RATE_LIMIT_REQUESTS=30
RATE_LIMIT_WINDOW=60

# Database Settings (for future PostgreSQL integration)
# DATABASE_URL=postgresql://user:password@localhost:5432/medchat

# Redis Settings (for WebSocket scaling)
# REDIS_URL=redis://localhost:6379

# Medical Compliance Settings
AUDIT_LOGGING=true
DATA_RETENTION_DAYS=2555  # 7 years for medical records

# Email Settings (for notifications)
# SMTP_SERVER=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USERNAME=your-email@domain.com
# SMTP_PASSWORD=your-app-password